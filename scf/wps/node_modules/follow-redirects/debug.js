var debug;
try {
  /* eslint global-require: off */
  debug = require("follow-redirects/node_modules/debug")("follow-redirects");
}
catch (error) {
  debug = function () { /* */ };
}
module.exports = debug;
